In directory `/app` there is `main.py`.

It currently fails because of a missing import.

Fix the code so that:

- `get_today_iso()` returns a string in format `YYYY-MM-DD`
- `main()` returns the same date string
- running `python /app/main.py` exits successfully

Do not print anything from `main.py`.
The output must be provided via return values only.

You must change files only in `/app`.